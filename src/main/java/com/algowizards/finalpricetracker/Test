iterate list of products (main category algorithm)
    number it (number | product.getproductname)
    if this is unique
        add to a list (new 2D arraylist)

DataStructures.List1D<String> categoryList = new DataStructures.List1D<>(lookupItem.getColumn(3));

List<String> categoryList = lookupItem.getColumn(3);
Set<String> uniqueItemCategories = new HashSet<String>(categoryList);
List<String> finalUniqueItemCategories = new ArrayList<>(uniqueItemCategories);



Product product1
product product2